# Runtime dependencies for the crop-disease dataset preprocessing framework.
# Lower bounds are the oldest versions the code is tested against; upper bounds
# guard against breaking major releases.

# --- core numerics / tabular -------------------------------------------------
numpy>=2.1,<3
pandas>=2.2,<3
pyarrow>=18.0,<23          # image_manifest.parquet writer

# --- imaging -----------------------------------------------------------------
pillow>=11.0,<13
opencv-python-headless>=4.10,<5   # blur/contrast metrics, DCT for perceptual hashing

# --- configuration / validation ----------------------------------------------
PyYAML>=6.0,<7
pydantic>=2.9,<3

# --- statistics / splitting ---------------------------------------------------
scikit-learn>=1.5,<2
scipy>=1.14,<2

# --- deep learning ------------------------------------------------------------
torch>=2.9,<3
torchvision>=0.24,<1

# --- reporting ----------------------------------------------------------------
matplotlib>=3.9,<4
jinja2>=3.1,<4
reportlab>=4.2,<5

# --- ergonomics ---------------------------------------------------------------
tqdm>=4.66,<5
